[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "eventbiz"
version = "0.1.0"
description = "Glamorous custom app: two-company (Creations + Delights) events, rental, bakery, website"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
authors = [{ name = "Glamorous" }]

[project.urls]
Homepage = "https://www.glamorous.mw"
